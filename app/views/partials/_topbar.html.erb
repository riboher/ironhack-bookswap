<nav class="top-bar">
<ul class="menu">
	<% if !reader_signed_in? %>
		<li>
			<button class="sign">Sign in</button>
		</li>
		<li>
			<button class="log">Log in</button>
		</li>
	<% end %>
	<li>
		<%= link_to 'Browse books', root_path, class: 'browsing' %>
	</li>
	<% if reader_signed_in? %>
	<li class="add-book-menu">
		<form class="search-bar js-create-book" role="search">
		  	<input type="search" class="search-isbn" placeholder="Enter ISBN" />
		  	<button type="submit" class="isbn-submit"><i class="fa fa-search"></i></button>
		</form>
	</li>
	<li>
		<span>Ahoi <%= current_reader.first_name %></span>
		<%= image_tag(current_reader.avatar, size: '30x30', class: 'profile-pic') %>
		<%= link_to '', reader_path(current_reader.id), class: "my-profile" %>
	</li>
	<li>
		<%= link_to '', destroy_reader_session_path, method: 'delete', class: "sign-out" %>
	</li>
	<% end %>
</ul>
</nav>